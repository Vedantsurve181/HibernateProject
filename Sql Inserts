create database hibernate_login;

use hibernate_login;

create table users(
    id int primary key auto_increment,
    username varchar(50),
    password varchar(50)
);

insert into users(username,password)
values('admin','1234');
